class Solution:
    def countSeniors(self, details: List[str]) -> int:
        count1 = 0
        l = []
        for i in range(len(details)):
            s = details[i]
            for j in range(len(s)):
                if(j>=len(s)-2):
                    u = s[-4]
                    y = s[-3]
                    m = u+y
            l.append(m)
        g = [int(i) for i in l]
        for i in g:
            if(i>60):
                count1 = count1+1 
        return count1
        
